fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pytest>=8.0
httpx>=0.27
streamlit>=1.32
cryptography>=42.0
scikit-learn>=1.4
nltk>=3.8

# v2 upgrade path, blocked in the authoring environment (torch install
# exceeded the per-command execution limit there), see aui/fidelity/backend.py:
# sentence-transformers>=2.6
